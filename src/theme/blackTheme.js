import { createTheme } from "@mui/material";

export const blackTheme = createTheme({
    palette: {
        primary: {
            main: '#390099'
        },
        secondary: {
            main: '#390099'
        },
        otherColor: {
            main: '#000000'
        },
        backgroundColor: {
            main: '#f8f8f8'
        } 
    },
    typography: {
        fontFamily: 'Righteous'
    }
});
