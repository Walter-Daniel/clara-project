import { createTheme } from "@mui/material";

export const blackTheme = createTheme({
    palette: {
        primary: {
            main: '#0f0f0f'
        },
        secondary: {
            main: '#efd703'
        },
        otherColor: {
            main: '#272727'
        } 
    } 
});
